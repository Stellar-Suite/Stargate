# Stargate
 Hyperwarp backend server, serves frontend, runs backend for managing game processes. For more info on the project, see the [org README](https://stellar-suite.github.io).
## Usage
* Run `npm install` to install dependencies
* Run `npm start` to start the server
* Place your static assets in the `user_static` folder. Don't worry, they won't be added to git.
## Credits:
* Inter is used to render the placeholder poster image. 
